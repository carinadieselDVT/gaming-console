import { Box, Typography } from "@mui/material";

interface HapticScreenProps {
  message?: string;
}

function HapticScreen({}: HapticScreenProps) {
  return <>Screen Area</>;
}

export default HapticScreen;
